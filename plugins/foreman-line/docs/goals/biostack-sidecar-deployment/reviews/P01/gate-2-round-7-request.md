# P01 Narrow Gate 2 Round 07 Request

> Narrow Gate 2 P01 Round 07: I ratify `directive-recovery-01.md` at goal commit `d92592b35c52c1902b7054c63dc299f61e0aa981` and authorize one isolated P01 correction round starting from candidate `59102d3c8170968d2c9d316ec5b07a934042cee5`. Exact Allowed Files are `.github/workflows/research-sidecar-ci.yml`, `scripts/verify-research-sidecar-container.mjs`, `scripts/verify-research-sidecar-container.test.mjs`, `backend/research-sidecar/.dockerignore`, and `backend/research-sidecar/docs/PARCELS.md`. After a fresh Step 0, implement only the four confirmed controls: fail-closed image entrypoint and baked-environment enforcement; default-deny Docker build-context enforcement with a true context-absence regression; fail-closed TERM-fixture Docker queries; and unpredictable, preflight-proven invocation ownership that cannot adopt or remove a pre-existing container. Retain all previously closed P01 controls and residual disclosures. Run the complete deterministic verification chain and obtain fresh two-seat adversarial plus security review on the exact corrected commit. For the single real-Docker TERM verification only, an isolated helper may access official Debian repositories solely to acquire the `docker.io` client; no other external network access is authorized. No push, fetch, PR, merge, Azure or production mutation, provider/scientific/model call, protected-data use, secret creation/rotation/use, deployment, public ingress, or P02-P04 work is authorized. Any file-scope change, unexpected network destination, repeated failure, candidate-identity ambiguity, or other drift stops execution. P02 remains blocked until P01 is independently accepted. Gate 3A and Gate 3B remain ungranted.
