<?xml version="1.0" encoding="utf-8"?>
<configuration>
  <!-- Clear Windows-specific Visual Studio fallback package folders so the Linux
       Docker container does not attempt to resolve paths that don't exist there. -->
  <fallbackPackageFolders>
    <clear />
  </fallbackPackageFolders>
</configuration>
